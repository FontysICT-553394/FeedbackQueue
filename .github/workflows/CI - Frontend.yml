name: CI Frontend

# Trigger on:
#  Pull requests in main or release branch
#  Pushes to dev/frontend
on:
  pull_request:
    branches:
      - main
      - release/*
  push:
    branches:
      - dev/frontend

jobs:
  frontend:
    name: Frontend Build & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        # The frontend folder
        working-directory: FQ-Frontend
    steps:
      - uses: actions/checkout@v4

      # Set up Node
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

# Skip testing for now (is not installed yet/have no tests)
#      - name: Run Unit Tests
#        run: npm run test
        
      - name: Build Frontend
        run: npm run build
